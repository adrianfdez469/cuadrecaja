# 🛡️ QA: F-040 — encender el drenaje de `BUSINESS`

> Fase 2 del pipeline: el `implementer` ya vació `QAB_OUTBOX_WITHHELD_ENTITIES`. Este informe
> verifica los seis criterios de aceptación **ejecutando** contra QAB real, siguiendo el orden del
> § 6 del contrato. Las líneas base (B1-B6) y su procedencia están en
> `.agents/archive/F-040-qa-fase1.md` — no se repiten aquí, solo se contrastan.
>
> **No heredé ningún exit code ni ningún diff de nadie**: los tres del criterio 6 los corrí yo, y
> el diff de `src/` lo leí yo, antes de crear ningún probe (E-012).

## Veredicto

## ✅ APROBADO — `passes: true` autorizado

Los seis criterios quedaron verificados ejecutando contra un QAB real y una base de datos real,
sin dobles de prueba. Sin bloqueantes.

## Criterios de aceptación

| # | Criterio | Cómo lo verifiqué | Resultado |
|---|----------|-------------------|-----------|
| 1 | `QAB_OUTBOX_WITHHELD_ENTITIES` vacía, `withheld` baja a 0, sin línea de log | Primera corrida del probe § 3 tras el cambio: `REPORT {...,"withheld":[]}`; `grep -c "qab.outbox.withheld"` sobre el stdout de esa corrida → **0** | ✅ |
| 2 | El atraso `BUSINESS` drena solo y queda con `procesadoAt` puesto | La misma corrida reclamó las 5 filas `BUSINESS` (`12054,12092-12095`) y las marcó `procesadoAt` de una vez (una sola corrida bastó: B1=B2=5 < `QAB_OUTBOX_BATCH_SIZE`=500). Tras la corrida: `B2 = 0` (medido contra B2, no contra B1 — ver nota de riesgo A abajo) y `SELECT count(*) ... intentos>0 AND procesadoAt IS NULL` → **0** | ✅ |
| 3 | `processed`/`stale` correctos, `planOutboxAck` marca las dos como procesadas sin reintentar | Evento real `12101` (EUR→fuera) → `status:"processed"`. Fila sembrada `12102` (§4.2, `T_aplicado-1min`, lista vieja con EUR) → `results:[{"eventId":"12102","status":"stale"}]`, en `response.ok` y **no** en `failed`; `procesadoAt` puesto, `intentos=0`. Dos corridas más: `claimed:0` las dos veces, `intentos` sigue en 0, la fila no reaparece en ningún `SENT`. Control discriminante: la tienda online **sigue sin ofrecer EUR** después del `stale` | ✅ |
| 4 | Lote mixto `BUSINESS`+`PRODUCT` se aplica entero, sin `400 INVALID_BATCH` | La primera corrida (criterio 1/2) YA fue mixta, sin fabricar nada (riesgo C no aplicó con solo 5 filas): `SENT` trajo 5 `BUSINESS`+2 `PRODUCT` (`12099,12100`) en el mismo lote; `RECEIVED kind:"ok"` (nunca `error`/`HTTP:400:`); los 7 `eventId` aparecen cada uno con su propio `status:"processed"` en `results[]`; los dos `PRODUCT` terminaron con `procesadoAt` puesto, sin efecto colateral | ✅ |
| 5 | Desactivar una moneda dejar de mostrarse en la tienda online (contra el otro lado) | Verificado en dos pasos, DESPUÉS del criterio 2 (nunca contra el `[]` inicial): (a) tras el criterio 2, `GET /tienda-principal` mostraba `data-ref-choices="EUR"` — la tienda ya ofrecía EUR; (b) `PUT .../monedas/EUR {activo:false}` (fresco) → drenado → `GET /tienda-principal` ya **no** trae ningún `data-ref-choices` — EUR desapareció. Confirmado también en la fila `Business` de QAB: `displayCurrencies` pasó de incluir EUR a `["CUP","GBP"]` | ✅ |
| 6 | Suite entera en verde tras vaciar la lista | `npx vitest run` → exit **0**, 208 archivos/4291 tests/1 skip (idéntico a B6); `npx tsc --noEmit` → exit **0**; `npm run lint` → exit **0**, 0 `Error:`. Los tres corridos **con el probe todavía sin crear**. `src/__tests__/qabOutboxWithheld.test.ts` sin tocar (`git diff --stat -- src/__tests__/` vacío) | ✅ |

### Notas obligatorias por criterio

- **Criterio 2, riesgo A (§10):** se midió contra **B2**, no contra B1, tal como exige el
  contrato. En esta ronda ambos valen 5 y el resultado no cambia, pero lo dejo explícito: la
  consulta que importa es la que filtra `tiendaOnlineHabilitada=true AND intentos<6` (B2), no el
  conteo bruto (B1).
- **Criterio 3:** confirmado que es un `stale` real y no un `duplicate` — el `eventId` sembrado
  (`12102`) es nuevo (autoincremento, nunca visto por QAB), no una reentrega del mismo `eventId`.
  El `updatedAt` sembrado (`T_aplicado - 1 minuto`, con `T_aplicado` = `updatedAt` del evento
  `12101` que dejó `["CUP","GBP"]` aplicado) es estrictamente **menor** que lo que QAB ya tenía
  guardado, que es la condición exacta que dispara la guarda anti-rancio. Encadenado con el
  criterio 5 (la lista sembrada **incluye EUR**, que el criterio 5 acababa de quitar): si la guarda
  estuviera rota, EUR habría reaparecido en la tienda, y no reapareció.
- **Criterio 4, riesgo C (§10):** comprobado, no supuesto — con solo 5 filas `BUSINESS` (muy por
  debajo del `LIMIT` de 500), no hubo bloqueo de cabecera y el lote mixto con los `PRODUCT`
  `12099`/`12100` se dio en la primera corrida sin que tuviera que fabricar nada aparte.
- **Riesgo B (§10):** inspeccionado **inmediatamente después de la primera corrida**, antes de
  seguir: `SELECT intentos, "ultimoError", count(*) ... GROUP BY` sobre las `BUSINESS` pendientes
  devolvió **`[]`** (vacío) porque las 5 ya no estaban pendientes — las cinco terminaron
  `procesadoAt` no nulo con `intentos=0` en la misma corrida, la mejor salida posible: nunca hubo
  ninguna fila con `intentos>0` que obligara a parar.
- **Security-guardian (última sección de `.agents/security/F-040.md`):** en el stdout de las 5
  corridas del drenaje (criterios 1, 2, 4, 5 y 3) — `grep -il "403\|BUSINESS_MISMATCH"` → sin
  coincidencias; `grep -il "bearer\|token"` → sin coincidencias. Ninguna línea de log llevó token
  ni el payload completo de un evento (el probe solo imprime `eventId`+`entity` en `SENT`, y el
  `outcome` de QAB —sin el payload de origen— en `RECEIVED`).

## Calidad de los tests

| Test | ¿Prueba código real? | ¿Falla si rompo la implementación? | Notas |
|------|---------------------|-----------------------------------|-------|
| `src/__tests__/qabOutboxWithheld.test.ts` | Sí — importa `QAB_OUTBOX_WITHHELD_ENTITIES`/`QAB_OUTBOX_DRAINABLE_ENTITIES` reales de `src/constants/qab.ts` | Sí, en la dirección que importa aquí: si alguien reintrodujera `BUSINESS` en la lista retenida, el caso de cobertura de `DRAINABLE` (que exige las seis entidades) fallaría | El `dev-tester` no escribió nada en este feature (contrato § 8, con los cuatro candidatos descartados uno a uno) — confirmado con `git diff --stat -- src/__tests__/` vacío. El único archivo relevante ya existía de F-027 y sigue en verde sin haber cambiado una línea: es exactamente lo que el criterio 6 pedía demostrar |

No hay tests nuevos que auditar. Los seis criterios de este feature no se pueden verificar con un
test unitario (todos necesitan una base de datos con atraso real y un QAB real al otro lado, tal
como el propio contrato argumenta en su § 8) — por eso están verificados aquí, ejecutando, y no en
`src/__tests__/`.

## Suite

- `npm test` (`npx vitest run`): **208/208 archivos, 4291/4291 tests, 1 skip preexistente** — exit 0
- `npx tsc --noEmit`: ✅ (exit 0)
- `npm run lint`: ✅ (exit 0, 0 `Error:`, solo warnings preexistentes ajenos a este feature)
- Sin rutas absolutas en `.claude/agents/` ni `.claude/skills/`: ✅ (F-040 no tocó ningún archivo
  de agente; `grep -rnE '(/Users/|/home/|~/|[A-Z]:\\)' .agents/ .claude/ --include='*.md'
  --include='*.json'` solo encuentra comandos de ejemplo y documentación histórica de E-001, ya
  presentes antes de este feature)
- `git diff --stat`: **un solo archivo**, `src/constants/qab.ts` (16 inserciones / 18 borrados,
  todas dentro del bloque de `QAB_OUTBOX_WITHHELD_ENTITIES` y su docstring). Comparé el docstring
  final byte a byte contra el literal del § 1.3 del contrato: coincide exacto.

## Aislamiento multi-tenant

No aplica reverificación estructural (el propio `security-guardian` lo señala: el camino de
emisión no cambia de forma, solo empieza a recibir eventos `BUSINESS` reales). Lo que sí verifiqué
ejecutando, y es lo que pedía la última sección de su informe: cero apariciones de `403
BUSINESS_MISMATCH` en las cinco corridas reales del drenaje, y cero tokens/payloads en los logs.

## Bloqueantes

Ninguno.

## Errores encontrados

Ninguno nuevo que amerite ficha en `.agents/errors/` — todo lo no anticipado por el contrato
(el backlog no-`BUSINESS` agotado, el cierre de la tienda del lado de QAB) ya se documentó y
resolvió en la fase 1/1b (`.agents/archive/F-040-qa-fase1.md` §§ 5 y 7/10), y no reapareció aquí.

## Higiene de cierre

- Probes borrados: `qaf038-sql.ts`, `qaf038-probe.ts`, `qaf038-sql-exec.ts` (cuadrecaja) y
  `qaf038b-qab-sql.ts` (queandabuscando, usado solo para lecturas de solo-SELECT contra `Store`
  y `Business`). `git status --porcelain` sin rastro suyo en los dos repositorios.
- `git diff -- src/` de cuadrecaja: solo `src/constants/qab.ts` (el cambio del `implementer`,
  intacto).
- Estado final relevante para quien retome: las 5 filas `BUSINESS` originales están todas
  `procesadoAt` no nulo; `Negocio Demo` en QAB tiene `displayCurrencies: ["CUP","GBP"]`; el
  backlog agotado (55 `PRODUCT`/8 `CATEGORY`/4 `CURRENCY`/2 `EXCHANGE_RATE`/3 `STORE`, todos
  `intentos=6`) sigue intacto y sin tocar, tal como pidió el coordinador.
