/** JWT del enlace /activar?token=... tras formulario landing. */
export const LANDING_ACTIVATION_JWT_EXPIRES_IN = "48h" as const;

/** Texto humano para UI (mantener sincronizado con expiresIn). */
export const LANDING_ACTIVATION_TTL_LABEL = "48 horas" as const;
